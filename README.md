# Buscador de Noticias

Este proyecto implementa una aplicación web utilizando Python y tecnologías de Web Semántica (RDF/Turtle).

## 📋 Requisitos Previos

Para ejecutar este proyecto necesitas tener instalado:
* **Python 3.8** o superior.
* Un navegador web (Chrome, Firefox, Edge).

## 🚀 Instalación y Configuración

Sigue estos pasos para ejecutar el proyecto en tu computadora local:

# Crea y activa tu entorno vitual

# En Windows:
python -m venv .venv
.\.venv\Scripts\activate

# En Mac/Linux:
python3 -m venv .venv
source .venv/bin/activate

# Instala las librerias necesarias para la ejecucion del pryecto, esto una vez ya activado el entorno virtual con el siguiente comando

pip install -r requirements.txt